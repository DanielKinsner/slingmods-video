# SlingMods Video Project — Claude orientation

This is one SlingMods video project, built from `M:\_template`.

**Do not rely on this file for rules — it's a pointer.** The law lives in one place:

1. **WORKFLOW-RULES.md** in the `slingmods-video` repo (Documents\GitHub\slingmods-video on the
   office PC; github.com/DanielKinsner/slingmods-video) — drive roles, backup gates, handoff order.
2. **STATUS.md** in the same repo — current pipeline state.
3. The `slingmods-video` skill — invoke it for the pipeline contract before doing anything here.
4. `README.md` in this folder — this project's info block and the folder map.

Quick reminders that never change: work happens on E:, M: is hands-off until Dan specifies,
externals are never touched, Dan approves scripts and finesses every install cut.
